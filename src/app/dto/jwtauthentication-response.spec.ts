import { JWTAuthenticationResponse } from './jwtauthentication-response';

describe('JWTAuthenticationResponse', () => {
  it('should create an instance', () => {
    expect(new JWTAuthenticationResponse()).toBeTruthy();
  });
});
